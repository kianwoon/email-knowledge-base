# Application settings
DEBUG=False
ENVIRONMENT=development  # or 'production'

# URLs and Ports
DEFAULT_PROD_URL=https://email-knowledge-base-2-automationtesting-ba741710.koyeb.app
DEFAULT_DEV_BACKEND_PORT=8000
DEFAULT_DEV_FRONTEND_PORT=5173
BACKEND_URL=  # Optional: override the auto-generated URL
FRONTEND_URL=  # Optional: override the auto-generated URL

# Microsoft OAuth2 settings
MS_CLIENT_ID=  # Required: your Azure AD application client ID
MS_CLIENT_SECRET=  # Required: your Azure AD application client secret
MS_TENANT_ID=  # Required: your Azure AD tenant ID
MS_REDIRECT_URI=  # Optional: override the auto-generated callback URL
MS_AUTH_BASE_URL=https://login.microsoftonline.com
MS_SCOPE=User.Read Mail.Read offline_access  # Space-separated list of scopes

# JWT settings
JWT_SECRET=  # Required: your JWT signing secret
JWT_ALGORITHM=HS256
JWT_EXPIRATION=3600  # Token expiration in seconds

# OpenAI settings
OPENAI_API_KEY=  # Required: your OpenAI API key
LLM_MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small

# Qdrant settings
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=  # Required for production
QDRANT_COLLECTION_NAME=email_knowledge

# Email processing settings
MAX_PREVIEW_EMAILS=10
EMBEDDING_DIMENSION=1536 